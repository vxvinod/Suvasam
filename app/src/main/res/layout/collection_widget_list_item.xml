<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:paddingLeft="@dimen/collection_padding"
    android:paddingRight="@dimen/collection_padding"
    android:paddingStart="@dimen/collection_padding"
    android:paddingEnd="@dimen/collection_padding"
    android:minHeight="@dimen/collection_padding"
    android:weightSum="2"
    android:id="@+id/widgetItemContainer"
    android:layout_height="wrap_content">

    <TextView android:id="@+id/widgetEventList"
        android:layout_width="wrap_content"
        android:gravity="start"
        android:layout_weight="1"
        android:textSize="18dp"
        android:text="Sample test"
        android:layout_gravity="center_vertical"
        android:layout_height="wrap_content"></TextView>

</LinearLayout>